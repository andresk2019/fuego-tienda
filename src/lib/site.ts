// URL pública de la tienda — un solo lugar para no repetir el literal
// en layout.tsx (metadataBase), sitemap.ts y robots.ts. Si el dominio
// cambia (ej. uno propio en vez de *.vercel.app), este es el único
// sitio que hay que tocar.
export const SITE_URL = "https://fuego-tienda.vercel.app";
